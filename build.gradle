apply plugin: 'groovy'
apply plugin: 'maven'

version 1.0.0'
group 'org.bonitasoft.migration.plugin'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile gradleApi()
    compile localGroovy()
}
